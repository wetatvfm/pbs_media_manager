{#
/**
 * @file show.html.twig
 * Default theme implementation to present Show data.
 */
#}
<div{{ attributes.addClass('show') }}>
    <h2>{{ title }}</h2>
    <ul>
        <li>Slug : {{ slug }}</li>
        <li>ID : {{  id }}</li>
    </ul>
    <p>{{ description_short }}</p>
    <p>{{ description_long }}</p>
</div>
